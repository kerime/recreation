# 解析結果

対象フォルダ: `C:\Program Files (x86)\AliceSoft\ドーナドーナ いっしょにわるいことをしよう`

## 1. ファイル構成

- `AliceStart.ini`: 起動設定。`BootName=dohnadohna`、`CodeName=dohnadohna.ain`、セーブフォルダは `SaveData`。
- `Version.txt`: `1.00`。
- `dohnadohna.exe`: ゲーム本体。
- `startup.exe`: 起動・設定ランチャー相当。
- `dohnadohna.ain`: AINスクリプト。先頭マジックは `AI2`。
- `dohnadohnaCG.afa`: CG/画像系と思われる大容量AFAアーカイブ。先頭マジックは `AFAH`。
- `dohnadohnaSound.afa`: 音声・効果音系と思われるAFAアーカイブ。
- `dohnadohnaVoice.afa`: ボイス系と思われるAFAアーカイブ。
- `dohnadohnaPact.afa`: 小容量のパックデータ。
- `dohnadohnaFont.fnl`: フォントデータ。
- `dohnadohnaEx.ex`: 拡張データ。先頭に `HEAD` / `EXTF` / `DATA`。
- `Manual/*.html`: Shift_JISのHTMLマニュアル。ゲームルール解析の主材料。
- `おまけ/User`: カスタム登場キャラクター/来場者のサンプルデータ。

## 2. データ形式の観察

- `.ain` は AliceSoft 系エンジンで使われるスクリプトコンテナと推定。先頭が `41 49 32 00`、つまり `AI2`。
- `.afa` は `41 46 41 48`、つまり `AFAH` で始まるアーカイブ。
- `.ex` は `HEAD`、`EXTF`、`DATA` セクションを持つ追加データ形式。
- マニュアル画像は通常の `jpg/png/gif` だが、ブラウザ再現版では著作物の複製を避けるため未使用。

## 3. ループ構造

1. 部室フェイズ
   - ToDoとTipsを見て進行。
   - 交流でメンバーのフィーリング上昇。
   - 活動P、スケジュール、クラブメイト、メンバーを管理。

2. クラブメイト管理
   - APL、TEC、FOC、DEF、個性、状態を持つ。
   - TPを使って練習。
   - Day経過でTPが全回復。

3. イベント運営フェイズ
   - 来場者は熱量、好み、リアクションを持つ。
   - クラブメイトのAPLと個性適性で注目度が変わる。
   - 成功すると活動P増加、TEC上昇、APL/FOC消耗。

4. チャレンジフェイズ
   - マップの1マス先を選んで進行。
   - ライバル、強豪、イベント、アイテム、クリアなどのマスがある。
   - チーム対戦はSPD/TECの影響を受け、HP/MP/VPを使う。

5. メンバー管理
   - POW、TEC、SPD、HP、MP、VP、フィーリングを持つ。
   - MPはマイナスでもスキル使用可能だがペナルティ。
   - VPはダメージの授受で増え、強化技に使える。

## 4. ブラウザ再現版の実装範囲

- 実装済み:
  - 新規生成した背景/肖像素材を使った全画面ビジュアル化。
  - 部室背景: `assets/hub_bg.png`。
  - バトル/マップ背景: `assets/battle_bg.png`。
  - メンバー/クラブメイト肖像: `assets/portraits_sheet.png`。
  - 画面遷移、カードホバー、スキャンライン、粒子、発光UI、シャイン演出。
  - 部室画面、活動P、Day、TP、スケジュール。
  - クラブメイト一覧、練習、休養、ステータス変動。
  - メンバー一覧、HP/MP/VP、フィーリング、回復。
  - 来場者マッチング型のイベント運営フェイズ。
  - 5x3チャレンジマップ、隣接移動、イベント、ボーナス、クリア。
  - `pm06_3.jpg` から読み取ったUI構造を中高生向けに再設計したチーム対戦画面。
  - 左右隊列、縦コマンド、ターンバナー、速度キュー、スキル窓、対象選択UI。
  - 速度順バトル、弱点、MP消費、MPマイナス時ペナルティ、VP技、ライバル自動行動、スカウトイベント。
  - CSSで作成したショットライン、ヒットリング、ダメージ数字、ボルテージ発光演出。
  - localStorageによるセーブ/ロード。

- 意図的に未実装:
  - 元ゲームの画像、音声、台詞、固有シナリオ、固有キャラクターの複製。
  - 成人向け描写。
  - AFA/AINの完全展開やバイトコード逆コンパイル。

## 5. 起動方法

`index.html` をブラウザで開く。静的ファイルのみで動作する。
